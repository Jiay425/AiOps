# CodeOps evaluation fixture provenance

All files below are deterministic test/simulation data for the CodeOps evaluation harness. They are not production telemetry, credentials, or claims about a real incident. Expansion Cases may reuse one of these evidence bundles when the business question, expected decision, or repair boundary is different; each Case records its `fixtureReference` and `fixtureReuseFrom` metadata.
